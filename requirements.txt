streamlit>=1.32.0,<2.0
# starlette 1.x breaks Streamlit's GZip middleware ("GZipResponder.__init__() missing
# 'thread_minimum_size'"). Pin <1.0 so the app loads in the browser at all.
starlette>=0.40.0,<1.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=6.1.1
scipy>=1.10.0
kaleido>=1.0.0
# required by pandas DataFrame.style.background_gradient (similarity matrix)
matplotlib>=3.7.0
charset-normalizer>=3.0.0
# Optional but recommended for encoding & peak analysis robustness
chardet>=5.0.0
